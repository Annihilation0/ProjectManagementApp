<!--Form for displaying information about the project-->
@model ProjectManagementApp.Models.ViewModels.ProjectEmployeesViewModel
@{
    var projectemployees = Model;
    var project = projectemployees.Project;
    var employees = projectemployees.Employees;
}

<!DOCTYPE html>
<html>
<head>
    <title>Projects</title>
</head>
<body>
    <h1>Project info
        
    </h1>

    <div class="container">
        <table class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>Project ID</th>
                    <th>Project Name</th>
                    <th>Client Company</th>
                    <th>Execution Company</th>
                    <th>Project Manager</th>
                    <th>Start Date</th>
                    <th>End Date</th>
                    <th>Priority</th>
                </tr>
            </thead>
            <tbody>               
                    <tr class="project-row" data-project-id="@project.ProjectID">
                        <td>@project.ProjectID</td>
                        <td>@project.ProjectName</td>
                        <td>@project.ClientCompanyName</td>
                        <td>@project.ExecutionCompanyName</td>
                        <td>@project.ProjectManagerFullName</td>
                        <td>@project.StartDate</td>
                        <td>@project.EndDate</td>
                        <td>@project.Priority</td>
                    </tr>               
            </tbody>
        </table>
    </div>

    <h1>Project employees</h1>

    <div class="container">
        <table class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>Employe ID</th>
                    <th>Full Name</th>
                    <th>Company</th>
                    <th>Email</th>
                    <th>Role</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var employee in employees)
                {
                    <tr class="employee-row" data-employee-id="@employee.EmployeeID">
                        <td>@employee.EmployeeID</td>
                        <td>@employee.FullName</td>
                        <td>@employee.Company.CompanyName</td>
                        <td>@employee.Email</td>
                        <td>@employee.Role</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>


    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(document).ready(function () {
            $('.project-row').click(function () {
                var projectId = $(this).data('project-id');
                window.location.href = '/Project/Details?projectId=' + projectId;
            });
        });

        $(document).ready(function () {
            $('.employee-row').click(function () {
                var employeeId = $(this).data('employee-id');
                window.location.href = '/Employee/Details?employeeId=' + employeeId;
            });
        });
    </script>
</body>
</html>

